#include <iostream>

int main () {
	
	int tab[5];
	std::cin >> tab[0];
	std::cin >> tab[1];
	std::cin >> tab[2];
	std::cin >> tab[3];
	std::cin >> tab[4];

	std::cout <<tab[0] + tab[1] + tab[2] + tab[3] + tab[4] + 5 <<std::endl;

	return 0;
}
